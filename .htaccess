<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Jika file benar-benar ada di root, jangan redirect
    RewriteCond %{REQUEST_FILENAME} !-f
    
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
